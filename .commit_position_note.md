# Commit Position 검증 (2026-07-01)

## 결론: 안전

DecisionRepository의 각 메서드는 독립 커넥션을 사용하고 즉시 commit한다.

```
begin_evaluation()
  → create_candidate()
      get_conn() → INSERT candidates → INSERT event_store → commit → put_conn()
  ← 커넥션 반환됨

[SMC 계산 — 열린 트랜잭션 없음]

record_rejection()
  → freeze_decision()
      get_conn() → INSERT decision_ledger → UPDATE candidates → INSERT event_store → commit → put_conn()
  ← 커넥션 반환됨
```

## 우려했던 시나리오 (해당 없음)

```
get_conn() → INSERT → [SMC 계산 중 lock 유지] → commit  ← 이 패턴이 아님
```

## 향후 주의 사항

Step 3 (record_order) 구현 시 execute_buy() 내부에서 호출될 경우,
execute_buy가 이미 열어둔 트랜잭션이 있다면 같은 커넥션을 공유하지 않도록
DecisionService는 항상 독립 커넥션을 사용해야 한다.
