# VWAP_DATASET_REPORT — 5분봉 데이터셋 구축

Iteration 6-1 · 2026-08-03
Regression **495 PASS / 16 Known FAIL** — RELEASE ALLOWED
**운영 코드 변경 0건 · 전략 무변경**

---

## 터미널 출력

```
==============================
VWAP DATASET BUILD
==============================
Target Symbols     112
Collected          112
Failed             0
Retry              0
Bars               2,056,648
Start Date         2025-08-01 09:00:00+09:00
End Date           2026-07-31 15:30:00+09:00
Coverage %         100.0%
Validation PASS    112
Validation FAIL    0
Elapsed Time       795.3s
==============================
BACKTEST READY     YES
==============================
```

---

## 1. 유니버스 — 하드코딩 없음

```sql
SELECT DISTINCT stock_code FROM trades WHERE stock_code ~ '^[0-9]{6}$'
```

**운영이 실제로 거래한 종목**을 DB 에서 뽑았다.

| 항목 | 수 |
|---|---|
| `trades` 전체 종목코드 | 115 |
| 6자리 숫자 (실 종목) | **112** |
| 제외 | `TEST001` · `TEST01` · `TEST99` |

⚠️ 제외된 3건은 **운영 원장에 섞여 있는 테스트 데이터**다
(Iteration 4 에서 발견, 아직 미정리). 정규식이 걸러냈다.

VWAP+AI 매수 종목 93개 중 91개가 실 종목이고 **전부 수집됐다**
(나머지 2개는 `TEST01`/`TEST99`).

### 조건검색을 지금 실행하지 않은 이유

지금 조건검색을 돌리면 **오늘 시점의 매칭**이 나온다.
2025-08 ~ 2026-07 백테스트에 그걸 쓰면 미래를 아는 셈이다
(look-ahead). 그래서 **그 기간에 실제로 거래된 종목**을 썼다.

---

## 2. 수집

| 항목 | 값 |
|---|---|
| API | 키움 `ka10080` (운영과 동일) |
| 틱 | 5분 (운영 `tic_scope="5"` 와 동일) |
| 요청 간격 | 0.30s (실측 안전선 0.20s + 여유) |
| 연속조회 | `cont-yn` / `next-key`, 최대 40페이지 |
| 재시도 | 최대 5회 지수 백오프 |
| **실제 재시도** | **0회** |
| **429 발생** | **0회** |
| 소요 | 795.3초 (13.3분) |

**112종목 전부 1회 성공. 재시도 0.**

### 운영 프로세스 영향

수집 중 `main_auto_trading.py` (PID 3339509) 가 가동 중이었다.
토큰을 공유하므로 영향을 확인했다.

- 장외 시각(05:0x) 에 실행 — 감시 루프가 대기 상태
- 당일 운영 로그에 **429 · rate limit 기록 0건**
- 프로세스 정상 유지 (uptime 2일 20시간)

⚠️ 스크립트에 **장중 실행 거부**를 넣었다 (`--force` 로만 우회).

---

## 3. 정규화

### 가격 부호 접두 제거

```python
def _px(v):
    s = str(v).strip().replace('+', '').replace('-', '')
    return abs(float(s))
```

⚠️ 키움은 `"cur_prc": "+262500"` 처럼 부호를 붙인다.
**하락 시 `-` 가 붙어 그대로 `float()` 하면 음수 가격**이 된다.
Iteration 6-0 실측에서 확인한 함정이다.

**결과: 음수 가격 0건.**

### 시간대

```
20260731153000  →  2026-07-31 15:30:00+09:00 (Asia/Seoul)
```

UTC 변환 **금지**. 장중 시각 판정(09:00 시작 · 15:30 종료 ·
14:50 오버나이트 차단)이 로컬 기준이다.

⚠️ 기존 `data/raw/396500_KS_5m.csv` 는 UTC 다. **섞으면 9시간
어긋난다.** 이번 데이터셋은 전부 KST 로 통일했다.

### 자료형

| 필드 | 형 |
|---|---|
| `datetime` (index) | `DatetimeIndex[ns, Asia/Seoul]` |
| `open` `high` `low` `close` | `float64` |
| `volume` `acc_volume` | `int64` |

---

## 4. 검증 — 전 항목 0건

| 항목 | 합계 | 목표 |
|---|---|---|
| 시간 역전 | **0** | 0 |
| 중복 봉 | **0** | 0 |
| OHLC 이상치 | **0** | 0 |
| 거래량 음수 | **0** | 0 |
| 결측(NaN) | **0** | 0 |
| 마지막 봉 정상 | **112/112** | 100% |
| **Validation PASS** | **112/112** | 100% |

### 결측을 채우지 않았다

⚠️ 거래가 없어 봉이 없는 것과 데이터가 빠진 것을 **구분할 수 없다.**
채우면 없는 거래를 만들어 낸다. 그래서 forward-fill 을 쓰지 않았다.

---

## 5. 데이터셋 현황

```
종목        112
총 봉수     2,056,648
기간        2025-08-01 09:00 ~ 2026-07-31 15:30 (KST)
용량        약 110MB
```

| 종목당 봉수 | 값 |
|---|---|
| 최소 | 15,273 (하이딥) |
| 중앙 | 18,653 |
| 최대 | 18,896 |

**전 종목이 2025-08-01 부터 시작한다.** 봉수 차이는 거래정지·
휴장·저유동성으로 봉이 안 생긴 날에서 온다 — 결측이 아니다.

### 시간외 봉

표본 확인 결과 시각 범위가 `09:00 ~ 16:35` 였다.
**정규장(15:30) 이후 봉이 존재한다** (1종목 표본 기준 155건).

⚠️ 백테스트에서 이 봉을 쓸지 말지는 **전략 정의 문제**다.
데이터는 그대로 두고 백테스트 단계에서 판단한다.
지금 잘라내면 되돌릴 수 없다.

---

## 6. 저장

```
phase1/minute_cache/5m/{code}.pkl     112개
phase1/minute_cache/meta.json         수집 메타
```

`meta.json`:
```json
{"built_at": "...", "tic": "5", "timezone": "Asia/Seoul",
 "symbols": 112, "collected": 112, "failed": 0,
 "bars": 2056648, "start": "2025-08-01 09:00:00+09:00",
 "end": "2026-07-31 15:30:00+09:00",
 "validation_pass": 112, "validation_fail": 0, "elapsed_sec": 795.3}
```

종목별 메타는 `minute_dataset_summary.csv` 에
`code · start_date · end_date · bar_count · last_update` 로 있다.

⚠️ `.pkl` 은 110MB 라 git 에 넣지 않았다 (`.gitignore`).
**재수집 가능한 산출물**이고, 스크립트가 커밋돼 있다.

---

## 7. 승인 기준

| 항목 | 목표 | 결과 | 판정 |
|---|---|---|---|
| 대상 종목 수집 | 100% | 112/112 | ✅ |
| 데이터 검증 PASS | 100% | 112/112 | ✅ |
| 중복 봉 | 0 | 0 | ✅ |
| 결측 봉 | 0 | 0 | ✅ |
| 시간대 통일 | 100% | 전부 Asia/Seoul | ✅ |
| Runtime Error | 0 | 0 | ✅ |
| Regression | PASS | 495 PASS / 16 Known FAIL | ✅ |
| **BACKTEST READY** | YES | **YES** | ✅ |

---

## 8. 남은 위험

1. **서버 보유 한계가 2025-08-01 이다.** 시간이 지나면 과거가 잘려
   나간다. **이 캐시가 실거래 구간을 담은 유일한 사본**이 될 수 있다.
   지우지 말 것.
2. **시간외 봉 처리 미결정.** 백테스트 단계에서 정해야 한다.
3. **거래정지·상장폐지 종목 미검증.** 112종목 전부 조회됐으나
   그 기간에 정지됐던 종목이 있는지 확인하지 않았다.
4. **운영 원장에 TEST 데이터 3건이 남아 있다.** 이번엔 정규식으로
   걸렀지만 다른 분석에서 또 걸러야 한다.
5. **수집 시점이 2026-08-03 이다.** 이후 데이터는 재수집이 필요하다.

---

## 9. 다음 — VWAP+AI Baseline 백테스트

데이터가 준비됐다. 다음은 합의된 프로세스를 그대로 적용한다.

```
목표 KPI 설정  →  Baseline 백테스트  →  Gap 분석
   →  병목 분석  →  수정 작업지시서  →  동일 조건 재백테스트
```

### 착수 전 확인이 필요한 것

**VWAP+AI 의 진입 규칙이 코드로 정의돼 있지 않다.**

Iteration 3 에서 확인한 것은 `entry_reason` 문자열
(`"VWAP 상향 돌파 (종합점수: 0.0)"`) 뿐이다.
백테스트하려면 아래를 코드에서 특정해야 한다.

- VWAP 계산 방식 (당일 누적? N봉 이동?)
- "상향 돌파" 판정 (종가 기준? 틱?)
- 종합점수 산출식 — **89건(53.9%)이 0.0 이었다**
- 진입 시각 제약
- 조건검색 통과 후 몇 봉 안에 진입하는가

**Iteration 6-2 를 "VWAP+AI 진입 규칙 코드 추적" 으로 두는 것이
맞다고 본다.** 규칙을 모르면 백테스트를 만들 수 없다.
